encoder_length: 12
decoder_length: 6
hidden: 16
n_heads: 2
n_layers: 1
dropout: 0.1
epochs: 2
batch_size: 8
lr: 1.0e-3
weight_decay: 1.0e-5
grad_clip: 1.0
seed: 0
device: cpu
val_fraction: 0.2
test_fraction: 0.2
checkpoint_dir: checkpoints
mlflow_tracking_uri: file:./mlruns
mlflow_experiment: graph-tt-smoke
wandb_project: graph-tt-smoke
