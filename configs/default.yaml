encoder_length: 12
decoder_length: 12
hidden: 64
n_heads: 4
n_layers: 3
dropout: 0.1
epochs: 30
batch_size: 32
lr: 1.0e-3
weight_decay: 1.0e-5
grad_clip: 1.0
seed: 20260514
device: cuda
val_fraction: 0.15
test_fraction: 0.15
checkpoint_dir: checkpoints
mlflow_tracking_uri: file:./mlruns
mlflow_experiment: graph-tt
wandb_project: graph-tt
